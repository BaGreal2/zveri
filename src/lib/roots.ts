export const modalRoot =
	typeof window !== 'undefined' ? document.getElementById('modal-root') : null;

export const searchRoot =
	typeof window !== 'undefined' ? document.getElementById('search-root') : null;
